import React from 'react';
import PropTypes from 'prop-types';

const ListTabsContainer = (props) => {
	console.log(props);
	return (
		<div>Todos go here</div>
	);
};

ListTabsContainer.propTypes    = {};
ListTabsContainer.defaultProps = {};

export default ListTabsContainer;
